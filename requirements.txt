anthropic>=0.40.0
pydantic>=2.7
pytest>=8.0
pytest-cov>=5.0
jupyter>=1.0
python-dotenv>=1.0

# Optional, only needed if LOADER_MODE=pdf (Phase 5 real-PDF swap):
# langchain-community>=0.3
# pypdf>=4.0
